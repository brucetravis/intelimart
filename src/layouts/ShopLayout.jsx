import React from 'react'

export default function ShopLayout({ children }) {
  return (
    <div>
        
        <div style={{ padding: '20px' }}>
            {children}
        </div>
    </div>
  )
}
